锁分解 锁分段的区别
	锁分解(分解成多少个，要根据具体情况来看，但似乎一般不会超过CPU核数)：当锁需要保
护多个相互独立的状态变量时，将锁分成多个不同的锁，从而减少原来在一个锁上的竞争，
在并发量适中的情况下可以很好的提高性能。
	说得更底层一点：就是原来N个线程抢一个CPU，变成N个线程抢多个CPU(分解成几个锁就抢几个CPU)
	锁分段：锁分解的最好状态是分解到CPU核数个锁，但这个一般不太可能。但ConcurrentHashMap采用另一种方式能更好的进行扩展

	
mybatis的插件的设计思想

连接池限制了线程池的使用